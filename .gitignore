# binario compilado
/maly

# Claude Code: estado local de cada máquina, no portable.
# settings.json es configuración por máquina y RESUME.md un checkpoint de sesión.
.claude/settings.json
.claude/RESUME.md
